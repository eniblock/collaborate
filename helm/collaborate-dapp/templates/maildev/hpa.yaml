{{- if .Values.maildev.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "collaborate-dapp.fullname" . }}-maildev
  labels:
    {{- include "collaborate-dapp.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "collaborate-dapp.fullname" . }}-maildev
  minReplicas: {{ .Values.maildev.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.maildev.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.maildev.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.maildev.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.maildev.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.maildev.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
