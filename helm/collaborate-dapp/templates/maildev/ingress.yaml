{{- if .Values.maildev.ingress.enabled -}}
{{- $fullName := include "collaborate-dapp.fullname" . -}}
{{- $svcPort := .Values.maildev.service.port -}}
{{- if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}-maildev
  labels:
    {{- include "collaborate-dapp.labels" . | nindent 4 }}
  {{- with .Values.maildev.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.maildev.ingress.tls }}
  tls:
    {{- range .Values.maildev.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.maildev.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            backend:
              serviceName: {{ $fullName }}-maildev
              servicePort: {{ $svcPort }}
          {{- end }}
    {{- end }}
  {{- end }}
