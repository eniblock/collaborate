apiVersion: v1
kind: Secret
metadata:
  name: {{ include "collaborate-dapp.keycloak.fullname" . }}
  labels:
    {{- include "collaborate-dapp.labels" . | nindent 4 }}
type: Opaque
data:
  {{- $keys := list "admin-password" "management-password" }}
  {{- include "collaborate-dapp.automaticSecret" (list . (include "collaborate-dapp.keycloak.fullname" .) $keys) | indent 2 }}
