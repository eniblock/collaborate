{{- if .Values.datasourceApiPeugeot.ingress.enabled -}}
{{- $fullName := include "fake-datasource.fullname" . -}}
{{- $svcPort := .Values.datasourceApiPeugeot.service.port -}}
{{- if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}-datasource-api-peugeot
  labels:
    {{- include "fake-datasource.labels" . | nindent 4 }}
  {{- with .Values.datasourceApiPeugeot.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.datasourceApiPeugeot.ingress.tls }}
  tls:
    {{- range .Values.datasourceApiPeugeot.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.datasourceApiPeugeot.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            backend:
              serviceName: {{ $fullName }}-datasource-api-peugeot
              servicePort: {{ $svcPort }}
          {{- end }}
    {{- end }}
  {{- end }}
