apiVersion: v1
kind: Service
metadata:
  name: {{ include "fake-datasource.fullname" . }}-datasource-iam
  labels:
    {{- include "fake-datasource.labels" . | nindent 4 }}
    app.kubernetes.io/component: datasource-iam
spec:
  type: {{ .Values.datasourceIam.service.type }}
  ports:
    - port: {{ .Values.datasourceIam.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fake-datasource.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: datasource-iam
