{{- if .Values.datasourceApiMobivia.ingress.enabled -}}
{{- $fullName := include "fake-datasource.fullname" . -}}
{{- $svcPort := .Values.datasourceApiMobivia.service.port -}}
{{- if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}-datasource-api-mobivia
  labels:
    {{- include "fake-datasource.labels" . | nindent 4 }}
  {{- with .Values.datasourceApiMobivia.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.datasourceApiMobivia.ingress.tls }}
  tls:
    {{- range .Values.datasourceApiMobivia.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.datasourceApiMobivia.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            backend:
              serviceName: {{ $fullName }}-datasource-api-mobivia
              servicePort: {{ $svcPort }}
          {{- end }}
    {{- end }}
  {{- end }}
