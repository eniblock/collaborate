{{- if .Values.datasourceDspA.ingress.enabled -}}
{{- $fullName := include "fake-datasource.fullname" . -}}
{{- $svcPort := .Values.datasourceDspA.service.port -}}
{{- if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}-api-dsp-a
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "fake-datasource.labels" . | nindent 4 }}
  {{- with .Values.datasourceDspA.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.datasourceDspA.ingress.tls }}
  tls:
    {{- range .Values.datasourceDspA.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.datasourceDspA.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            backend:
              serviceName: {{ $fullName }}-api-dsp-a
              servicePort: {{ $svcPort }}
          {{- end }}
    {{- end }}
  {{- end }}
